apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: flight-app-api
spec:
  minReplicas: 3
  maxReplicas: 15
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60

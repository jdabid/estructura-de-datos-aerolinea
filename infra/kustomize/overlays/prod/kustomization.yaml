apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml

namePrefix: prod-
namespace: flight-prod

patchesStrategicMerge:
  - configmap-patch.yaml
  - deployment-patch.yaml
  - hpa-patch.yaml

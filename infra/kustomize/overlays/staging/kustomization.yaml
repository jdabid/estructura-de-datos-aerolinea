apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: staging-
namespace: flight-staging

patchesStrategicMerge:
  - configmap-patch.yaml
  - deployment-patch.yaml
  - hpa-patch.yaml

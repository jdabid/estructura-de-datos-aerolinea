apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: flight-system

commonLabels:
  app.kubernetes.io/name: flight-app
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: flight-reservation-system

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - worker-deployment.yaml
  - service.yaml
  - hpa.yaml

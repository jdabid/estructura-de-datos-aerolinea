apiVersion: v1
kind: Service
metadata:
  name: flight-app-api
  labels:
    app.kubernetes.io/name: flight-app
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: flight-app
    app.kubernetes.io/component: api
